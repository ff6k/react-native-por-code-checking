import Splash from './Splash';
import {Routes} from '../../app-config/constants'

const SplashConfig = {
  name: Routes.SPLASH,
  component: Splash,
  options: {
    headerShown: false
  },
};

export default SplashConfig;
