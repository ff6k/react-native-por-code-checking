import ShareCode from './ShareCode';
import { Routes } from '../../../app-config/constants';

const ShareCodeConfig = {
    name: Routes.TICKETSSHARECODE,
    component: ShareCode,
    options: {
        headerShown: false,
    },
};

export default ShareCodeConfig;
