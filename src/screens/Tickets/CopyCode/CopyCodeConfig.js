import CopyCode from './CopyCode';
import { Routes } from '../../../app-config/constants';

const CopyCodeConfig = {
    name: Routes.TICKETSCOPYCODE,
    component: CopyCode,
    options: {
        headerShown: false,
    },
};

export default CopyCodeConfig;
