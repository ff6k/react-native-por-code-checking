import Success from './Success';
import { Routes } from '../../../app-config/constants';

const SuccessConfig = {
    name: Routes.PAYMENTSUCCESSSELECTPAYMENT,
    component: Success,
    options: {
        headerShown: false,
    },
};

export default SuccessConfig;
